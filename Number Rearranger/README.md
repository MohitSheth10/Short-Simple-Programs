Enter any number and this program prints its digits in reverse order.

Example: `12345` → `54321`

It works step by step: each digit goes into a list, and the list is read from the end back to the start. (Python's `[::-1]` shortcut can do the same thing in one line.)
